# Copy to `env` (gitignored, chmod 600). Sourced with `set -a` by bashrc-extra,
# so every line is exported.

# ANTHROPIC_API_KEY=
# GITHUB_TOKEN=
# TODO(bootstrap): the API tokens / registry credentials this environment needs.
